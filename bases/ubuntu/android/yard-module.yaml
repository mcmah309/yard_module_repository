# yaml-language-server: $schema=https://raw.githubusercontent.com/mcmah309/containeryard/master/src/schemas/yard-module-schema.json

description: "Base unbutu module with android utils. Should be the first module in the list."
args:
  optional:
    - android_platform
    - android_build_tools